version: '3.8'

services: 

    web-app:
        build: .
        ports:
            - "3000:3000"
            depends_on:
                - database

    database:
        image: mongo:latest
        ports:
            - "27017:27017"
